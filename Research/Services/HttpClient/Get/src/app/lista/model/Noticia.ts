// SuperClasse
// Exemplo de abstração, onde a classe tem as caracteristicas do objeto desejado,
// mas, apenas torna-se o mesmo quando uma classe filha a utiliza como base 
export class Noticia {
    id: number
    titulo: string
    descricao: string
    autor: string
    status: number
}